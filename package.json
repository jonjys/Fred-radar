{
  "name": "radar",
  "version": "0.3.0",
  "private": true,
  "description": "Radar – smarta rekommendationer och jämförelser av AI- och produktivitetsverktyg för svenska och nordiska användare.",
  "scripts": {
    "build:kategori": "node scripts/generate-kategori.js",
    "build:sitemap": "node scripts/generate-sitemap.js",
    "build": "npm run build:kategori && npm run build:sitemap",
    "serve": "npx --yes serve . -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
